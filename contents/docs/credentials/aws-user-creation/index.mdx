---
title: Creación de Usuario en AWS para Despliegues
description: Guía para crear un usuario en AWS con permisos adecuados para realizar despliegues con AWS CDK.
keywords: ["AWS IAM", "Usuario AWS", "Permisos AWS", "Despliegue CDK"]
---

# 👤 Creación de Usuario en AWS para Despliegues

Para poder realizar despliegues en AWS utilizando **AWS CDK**, es necesario contar con un usuario de AWS IAM con los permisos adecuados. A continuación, se describen los pasos para crear un usuario y asignarle permisos.

## 🔹 Paso 1: Acceder a la Consola de AWS IAM

1. Inicia sesión en la consola de **AWS**.
2. Dirígete a **IAM (Identity and Access Management)**.
3. En el menú lateral, selecciona **Usuarios**.
4. Haz clic en **Agregar usuario**.

## 🔹 Paso 2: Configurar el Usuario

1. Asigna un **nombre de usuario** (ejemplo: `cdk-deploy-user`).
2. Selecciona el tipo de acceso:
   - **Acceso programático** (para despliegues mediante CLI y SDKs).
   - **Acceso a la consola de AWS** (opcional, si también necesitas acceso manual).
3. Haz clic en **Siguiente: Permisos**.

## 🔹 Paso 3: Asignar Permisos

Para permitir despliegues con AWS CDK, el usuario debe tener permisos adecuados. Puedes asignarlos de dos formas:

### Opción 1: Adjuntar Permisos Administrativos (Recomendado para desarrollo)

1. En la pestaña **Adjuntar políticas existentes**, busca y selecciona:
   - `AdministratorAccess` (brinda acceso completo a AWS).
2. Haz clic en **Siguiente: Revisar**.

### Opción 2: Asignar Permisos Mínimos (Recomendado para producción)

Si prefieres limitar los permisos, crea una nueva política con los siguientes permisos esenciales.

1. En **Permisos**, haz clic en **Crear política**.
2. Guarda y asigna la política al usuario.

## 🔹 Paso 4: Generar Credenciales de Acceso

1. Una vez creado el usuario, selecciona **Crear clave de acceso**.
2. Guarda el **Access Key ID** y **Secret Access Key** en un lugar seguro.
3. Configura estas credenciales en tu entorno ejecutando:

```sh
aws configure
```

4. Ingresa las credenciales cuando sean solicitadas.

---

✅ **Listo!** Ahora el usuario puede realizar despliegues en AWS con CDK.
